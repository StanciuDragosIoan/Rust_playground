/*

Variables and mutability:

  ran:
    cargo new name
    cargo build
    cargo run name

In Rust variables are by default immutable (but we can make them mutable 
if needed by adding the word mut before them)

let x = 5; //immutable
let mut x = 5;//muttable


